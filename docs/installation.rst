============
Installation
============

At the command line::

    $ easy_install gitenberg.metadata

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv gitenberg.metadata
    $ pip install gitenberg.metadata
